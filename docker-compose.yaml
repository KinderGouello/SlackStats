version: '3'

services:
  app:
    build: docker
    # deploy:
    #   resources:
    #     limits:
    #       memory: 1g
    #     reservations:
    #       memory: 300M
    ports:
        - '9000:9000'
    volumes:
        - ./app:/home/node/app
    tty: true

  redis:
    image: redis:latest
    ports:
        - '6379:6379'

volumes:
  app:
    driver: local