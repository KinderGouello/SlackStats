version: '3'

services:
  app:
    build: docker
    # deploy:
    #   resources:
    #     limits:
    #       memory: 1g
    #     reservations:
    #       memory: 300M
    ports:
        - '3000:3000'
    volumes:
        - ./app:/home/nodejs/app
    tty: true

volumes:
  app:
    driver: local